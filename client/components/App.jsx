import React from 'react'
import Pix from './Pix'

const App = () => {
  array.map()
  let array = []
  for (let i = 0; i < 100; i++)
  array.push(<Pixel key={i}/>)
  return (
    <div>
      {array}
    </div>
  )
}

export default App